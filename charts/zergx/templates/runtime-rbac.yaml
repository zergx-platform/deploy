{{- /* OBSOLETE: ops-extension no longer talks to the k8s API directly —
       all runtime-namespace operations go through jjlab's /ops surface (the
       jjlab Role in the runtime namespace covers what jjlab needs). Kept as an
       empty template so chart checksums don't break. */ -}}
